# Cleanup Notes / クリーンアップメモ

- Removed unused helpers `CarNumberExtractor` and `MetaInfo` to reduce dead code and avoid maintaining unreferenced utilities.
  - 将来利用予定だったヘルパーがビルドに載らず放置されていたため、不要コードとして削除しメンテコストを下げました。
